const PlayerRoles = require('./roles');
const speedUps = require('./speedups');
const resources = require('./resources');

module.exports = {
    ROLES: PlayerRoles,
    SPEED_UPS: speedUps,
    RESOURCES: resources
};